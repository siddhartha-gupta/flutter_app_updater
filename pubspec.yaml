name: flutter_app_updater
description: Package to handle app force update
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.8.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  package_info: ^0.4.1
  version: ^1.2.0
  rxdart: ^0.24.0
  get_it: ^5.0.1
  url_launcher: ^5.7.8

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
